### Release 4.3.1:

- **Admin Registry "Container Images" tab** (`AdminImagesTab`) — server-side pagination, debounced search, per-image tag selector, security/pull badges, copy `docker pull` (single + "Pull All"); backed by the new `GET_SYSTEM_HARBOR_IMAGES` query.
- Application submission "name already taken" error handling + `IconUpload` validation feedback.
- Tests: `AdminImagesTab`-supporting query, submission errors, `IconUpload`.

